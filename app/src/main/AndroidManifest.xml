<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.navigationdrawer">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Camara -->
    <uses-feature android:name="android.hardware.camera.any" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!--Usar el permiso para la llamada -->
    <uses-permission android:name="android.permission.CALL_PHONE" />


    <!--Mapa -->

    <!-- Provides location accuracy to within a city block.-->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Provides a more accurate location than one provided when you request ACCESS_COARSE_LOCATION.
     This permission is necessary for some connectivity tasks, such as connecting to nearby
     devices over Bluetooth Low Energy (BLE). -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />


    <application
        android:name=".di.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NavigationDrawer">
        <activity
            android:name=".ui.planner.PlannerActivity"
            android:exported="false"
            android:label="@string/title_activity_planner"
            android:theme="@style/Theme.NavigationDrawer.NoActionBar" />
        <activity
            android:name=".ui.splashScreen.SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true" />
        <activity
            android:name=".ui.registrer.RegisterActivity"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>